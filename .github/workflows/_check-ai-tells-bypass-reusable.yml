name: Check AI Tells (Bypass)

on:
  workflow_call:

jobs:
  check-ai-tells:
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - name: Bypass check for non-prose PRs
        run: echo "No AI tells check required for this PR. Status check bypassed."
