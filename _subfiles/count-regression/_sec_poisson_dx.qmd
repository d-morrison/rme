### Residuals

#### Observation residuals

$$e_i \eqdef y_i - \ey_i$$

#### Pearson residuals

$$r_i \eqdef \frac{e_i}{\hse{e_i}} \approx \frac{e_i}{\sqrt{\ey_i}}$$

#### Standardized Pearson residuals

$$r_{p,i} \eqdef \frac{r_i}{\sqrt{1-h_i}}$$
where $h_i$ is the
[leverage](logistic-regression.qmd#def-leverage-glm)
of observation $i$
--- that definition indexes covariate patterns,
which here we take to be the individual observations.

#### Deviance residuals

$$
d_i \eqdef \signt(y_i - \ey_i)
\sqrt{2\sb{\llik_{\text{full}}(y_i) - \llik(\evb; y_i)}}
$$

:::{.callout-note}

$$
\signt(x) \eqdef
\begin{cases}
\frac{x}{|x|} & x \neq 0\\
0 & x = 0
\end{cases}
$$
In other words:

* $\signt(x) = -1$ if $x < 0$
* $\signt(x) = 0$ if $x = 0$
* $\signt(x) = 1$ if $x > 0$

::::{.content-hidden}

```{r}
plot(sign, xlim = c(-1, 1), xlab = "x", ylab = "sign(x)")
```

::::

:::
