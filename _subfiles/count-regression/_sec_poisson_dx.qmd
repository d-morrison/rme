### Residuals

#### Observation residuals

$$e \eqdef y - \ey$$

#### Pearson residuals

$$r \eqdef \frac{e}{\hse{e}} \approx \frac{e}{\sqrt{\ey}}$$

#### Standardized Pearson residuals

$$r_p \eqdef \frac{r}{\sqrt{1-h}}$$
where $h$ is the leverage value for observation $i$.

#### Deviance residuals

$$
d_i \eqdef \signt(y_i - \ey_i)
\sqrt{2\sb{\llik_{\text{full}}(y_i) - \llik(\eb \mid y_i)}}
$$

:::{.callout-note}

$$\signt(x) \eqdef \frac{x}{|x|} \quad \text{for } x \neq 0$$
In other words:

* $\signt(x) = -1$ if $x < 0$
* $\signt(x) = 0$ if $x = 0$
* $\signt(x) = 1$ if $x > 0$

::::{.content-hidden}

```{r}
plot(sign, xlim = c(-1, 1), xlab = "x", ylab = "sign(x)")
```

::::

:::
