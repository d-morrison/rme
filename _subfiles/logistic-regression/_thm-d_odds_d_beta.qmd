:::{#thm-d_odds_d_beta}
#### Gradient of odds w.r.t. coefficients

$$\derivf{\odds}{\vb} = \vx \cdot \odds$$

:::

::: proof
Applying the [vector chain rule](math-prereqs.qmd#thm-chain-vec) along with @lem-deriv-invodds and @lem-d_logodds-d_vb:

$$
\ba
\derivf{\odds}{\vb}
&= \red{\derivf{\eta}{\vb}} \teal{\derivf{\odds}{\logodds}}
&& \text{(vector chain rule, through } \logodds = \eta \text{)}
\\
&= \red{\vx} \teal{\derivf{\odds}{\logodds}}
&& \text{(gradient of log-odds w.r.t. coefficients: } \derivf{\eta}{\vb} = \vx \text{)}
\\
&= \red{\vx} \teal{\odds}
&& \text{(derivative of odds w.r.t. log-odds: } \derivf{\odds}{\logodds} = \odds \text{)}
\ea
$$
:::

{{< slidebreak >}}

:::{#exm-d-odds-d-beta}

#### Evaluating gradient of odds w.r.t. coefficients

Consider a subject with covariate vector $\vx = (1, 2)'$ and linear predictor $\eta = \vx'\vb = 0$, so $\odds = \exp(0) = 1$.

By @thm-d_odds_d_beta:

$$
\ba
\derivf{\odds}{\vb}
&= \vx \cdot \odds
&& \text{(@thm-d_odds_d_beta)}
\\
&= \pmat{1 \\ 2} \cdot 1
&& \text{(substitute } \vx = (1,2)' \text{ and } \odds = 1 \text{)}
\\
&= \pmat{1 \\ 2}
&& \text{(scalar multiplication)}
\ea
$$

The gradient vector indicates that increasing $\beta_0$ or $\beta_1$ increases the odds at rates 1 and 2, respectively.
:::

{{< slidebreak >}}

:::{#cor-d_odds_d_beta}

#### Gradient of odds w.r.t. coefficients in terms of probability

$$\derivf{\odds}{\vb} = \vx \cdot \frac{\pi}{1-\pi}$$

:::

::: proof
By @thm-d_odds_d_beta and the definition of odds in terms of probability $\pi$:

$$
\ba
\derivf{\odds}{\vb}
&= \red{\vx} \teal{\odds}
&& \text{(gradient of odds w.r.t. coefficients, by @thm-d_odds_d_beta)}
\\
&= \red{\vx} \frac{\pi}{1-\pi}
&& \text{(odds in terms of probability: } \odds = \frac{\pi}{1-\pi} \text{)}
\ea
$$
:::

{{< slidebreak >}}

:::{#exm-cor-d-odds-d-beta}

#### Gradient of odds in terms of probability

For a model where a subject has event probability $\pi = 0.5$ and covariate vector $\vx = (1, 3)'$:

$$
\ba
\frac{\pi}{1-\pi} &= \frac{0.5}{0.5} = 1
&& \text{(compute odds from probability)}
\\
\derivf{\odds}{\vb} &= \pmat{1 \\ 3} \cdot 1 = \pmat{1 \\ 3}
&& \text{(apply @cor-d_odds_d_beta)}
\ea
$$
:::
