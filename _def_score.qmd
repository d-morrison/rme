:::{#def-score}
#### Score function

The **score function** of a statistical model $\pr(\vec X=\vec x)$ is
the gradient (i.e., first derivative) of the log-likelihood of that model:

$$\score(\th) \eqdef \deriv{\th} \lik(\th)$$
:::
