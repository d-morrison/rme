Generalized linear models have three components:

1.   The **outcome distribution** family: $\p(Y|\mu(\vx))$

2.  The **link function**: $g(\mu(\vx)) = \eta(\vx)$

3.   The **linear component**: $\eta(\vx) = \vx \cdot \beta$

